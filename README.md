# Laravel PHP Framework

# Commands to install

install git
install git-lfs
take pull

composer install

create database
configure .env
php artisan migrate
php artisan db:seed
php -d memory_limit=3G artisan db:seed --class=PostalCodesTableSeeder